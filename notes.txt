
features:
    abstracts the response writing process 
    configuration
        encoding // Buffers
        logging

/resource/images/xyz  

allow a user to define a hierarchy / sequence of functions that our middleware runs

parameterizing routes

/users/:name

/resource/upload

function(req,res,next) {

}

/resource -> middleware
next()

/images -> middleware
 



var app = require('./frmwk');

app.config['response_encoding'] = 'utf-8';
app.config['log_path'] = __dirname + '/logs';
app.config['logger'] = false;


